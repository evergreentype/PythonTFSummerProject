import math, funClasses

class RectangleArea(funClasses.MathObject):
	def __init__(self):
		a = funClasses.Length
		b = funClasses.Length

		self.__value = (a, b)

	def find_value(self, *args):
		pass

